[build]
builder = "nixpacks"

[build.env]
PYTHON_VERSION = "3.11"

[build.nixpacks]
providers = ["python"]

[phases.build]
cmds = ["pip install -r requirements.txt", "python scripts/data_processing/build_games_db.py"]

[phases.start]
cmds = ["uvicorn app.main:app --host 0.0.0.0 --port $PORT"]
